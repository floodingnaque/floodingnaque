# PWA Icons

Generate PWA icons from the SVG favicon using:

```bash
npx pwa-asset-generator public/favicon.svg public/icons --icon-only --type png --padding "10%" --background "#3B82F6"
```

Required sizes: 72, 96, 128, 144, 152, 192, 384, 512 pixels.
