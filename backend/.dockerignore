# =====================================================
# Docker Ignore File for Floodingnaque Backend
# =====================================================
# Prevents unnecessary files from being copied to Docker image

# Git
.git
.gitignore
.gitattributes

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
# Note: .env.* files are excluded from the Docker image for security.
# For docker-compose: Use env_file directive (loads at container runtime)
# For standalone docker run: Use --env-file flag
# Example: docker run --env-file .env.production floodingnaque-api:latest
.env.development
.env.staging
.env.production
.env.local
!.env.example
.venv
venv/
ENV/

# Sensitive files - SECURITY
*.pem
*.key
# Keep .crt files - they're mounted at runtime for database SSL
# *.crt
secrets/
secrets*
*.secret
credentials/
.credentials
# Keep certs directory structure (files are mounted at runtime)
!certs/
!certs/.gitkeep

# Google Cloud credentials (NEVER include in Docker image!)
google_oauth_credentials.json
client_secret*.json
*-service-account.json
*-sa-key.json
floodingnaque-*.json
google_application_credentials*.json
*.iam.gserviceaccount.com.json
earth*.json
astral-archive-482008-g2-b4f2279053c0.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject
.settings/

# Testing
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
.pytest_cache/
nosetests.xml
coverage.xml
*.cover
.hypothesis/
pytest.ini

# Documentation (don't need in runtime)
docs/
*.md
!README.md

# Jupyter
.ipynb_checkpoints
*.ipynb

# Logs (will be created at runtime)
logs/
*.log

# Temp files
tmp/
temp/
*.tmp
*.temp

# OS files
.DS_Store
Thumbs.db

# Scripts not needed in production (training, evaluation, etc.)
scripts/
# Allow specific scripts needed in production image
!scripts/backup_database.sh
!scripts/backup_database.py
!scripts/purge_expired_ips.py

# Tests not needed in production image
tests/

# Evaluation results
evaluation_results/

# Backups
backups/
*.bak

# Database files
*.db
data/*.db

# Data subdirectories (raw/cleaned data not needed in image — mounted at runtime)
data/raw/
data/cleaned/
data/processed/

# Reports
reports/

# mypy cache
.mypy_cache/
mypy.ini

# Docker files (prevent nested copying)
Dockerfile*
docker-compose*
.dockerignore
